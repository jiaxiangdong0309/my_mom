# AI 知识记忆库 - 版本迭代规划

**版本**: v1.0
**日期**: 2025-12-25
**文档状态**: 待评审
**编写人**: 产品组

---

## 1. 文档概述

### 1.1 文档目的

本文档定义「AI 知识记忆库」项目的版本迭代路线，采用**从底层到上层、从简单到复杂**的渐进式开发策略，确保每个版本都有可交付的成果，技术风险逐步递增。

### 1.2 迭代原则

| 原则 | 说明 |
|------|------|
| **自底向上** | 先实现数据层，再实现业务层，最后实现展示层 |
| **简单先行** | 先用简单方案跑通流程，再引入复杂组件 |
| **增量验证** | 每个版本都要可测试、可演示 |
| **风险可控** | 高风险功能（向量、AI 联调）放在中后期 |
| **持续交付** | 每个版本结束都有可用的产出物 |

### 1.3 版本命名规则

- **v0.1 ~ v0.8**: 开发版本，用于内部测试
- **v1.0**: 正式发布的第一个稳定版本
- **v1.1, v1.2**: 小版本迭代，功能增强
- **v2.0**: 重大架构升级

---

## 2. 迭代路线图

```
v0.1 (Core层)     v0.2 (向量化)    v0.3 (API层)
    ↓                  ↓                ↓
  数据模型          Embedding        HTTP接口
  CRUD操作          ChromaDB         Swagger
    ↓                  ↓                ↓
v0.4 (CLI)  →   v0.5 (Skills)  →  v0.6 (WebUI)
    ↓                  ↓                ↓
 命令行启动        AI编辑器联调       可视化界面
    ↓                  ↓                ↓
v0.7 (异步)  →   v0.8 (打包)    →  v1.0 (发布)
    ↓              ↓                ↓
 任务队列       可执行文件      正式发布
```

---

## 3. 技术栈清单

根据[技术选型文档](./技术选型文档.md)，项目最终技术栈如下：

| 层级 | 技术选型 | 版本 | 用途 |
|------|---------|------|------|
| **语言** | Python | 3.11+ | 运行时 |
| **Web 框架** | FastAPI | 0.109+ | API 服务 |
| **ASGI 服务器** | Uvicorn | 0.27+ | 运行 FastAPI |
| **数据库（结构化）** | SQLite | 3.40+ | 元数据存储 |
| **ORM** | SQLAlchemy | 2.0+ | 数据库操作（异步） |
| **向量数据库** | ChromaDB | 0.4.22+ | 向量存储与检索 |
| **异步队列** | asyncio.Queue | 内置 | 任务队列（MVP） |
| **Embedding 本地** | sentence-transformers | 2.2+ | 本地向量化 |
| **Embedding 模型** | BAAI/bge-small-zh-v1.5 | 80MB | 向量模型（512维） |
| **Embedding 远程** | Jina AI | - | 备选方案 |
| **前端框架** | React | 18+ | UI |
| **前端语言** | TypeScript | 5+ | 类型安全 |
| **构建工具** | Vite | 5+ | 打包构建 |
| **UI 组件库** | Ant Design | 5+ | 组件 |
| **样式方案** | Tailwind CSS | 3+ | 原子化 CSS |
| **CLI 框架** | Typer | 0.9+ | 命令行 |
| **CLI 美化** | Rich | 13+ | 终端输出 |
| **Token 计算** | tiktoken | 0.5+ | 内容分段 |
| **HTTP 客户端** | httpx | 0.26+ | 异步 HTTP |
| **数据验证** | Pydantic | 2.5+ | 数据验证 |
| **打包工具** | PyInstaller | 6+ | 可执行文件 |

---

## 4. 详细版本规划

### v0.1 - Core 层基础

**目标**：跑通核心数据流程，不依赖任何 AI 能力

**时间**: 第 1 周

**技术范围**:
- ✅ 单一 SQLite 数据库（无向量）
- ✅ SQLAlchemy 2.0+ ORM（同步模式，v0.3 升级异步）
- ✅ 基础 CRUD 操作
- ✅ pytest 测试框架

**功能清单**:
```python
# 数据模型
class Memory:
    - id: int
    - title: str
    - content: str
    - tags: List[str]
    - created_at: datetime

# 核心方法
✅ create_memory(title, content, tags)
✅ get_memory(id)
✅ list_memories(filters)
✅ delete_memory(id)
✅ search_by_keyword(keyword)  # 纯文本搜索，LIKE 查询
```

**数据表设计**:
```sql
CREATE TABLE memories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    tags TEXT NOT NULL,  -- JSON: ["bug", "内存"]
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_memories_created ON memories(created_at DESC);
```

**验证方式**:
- 单元测试覆盖率 > 80%
- 命令行手动测试：`python3 -m core.main`
- 存储测试数据，验证增删查改

**交付物**:
- [core/__init__.py](core/__init__.py)
- [core/models.py](core/models.py) - SQLAlchemy 模型
- [core/crud.py](core/crud.py) - 数据库操作
- [core/database.py](core/database.py) - 数据库连接管理
- [tests/test_core.py](tests/test_core.py) - 测试用例

**成功标准**:
- [ ] 能够创建、读取、更新、删除记录
- [ ] 能够通过关键词搜索记录
- [ ] 所有测试通过（pytest + pytest-asyncio）
- [ ] 代码通过类型检查（mypy）

---

### v0.2 - 向量化能力

**目标**：集成 Embedding 和向量检索

**时间**: 第 2 周

**技术范围**:
- ✅ ChromaDB 0.4.22+ 集成
- ✅ sentence-transformers 2.2+ + BAAI/bge-small-zh-v1.5（本地模型，80MB）
- ✅ Embedding 生成和存储
- ✅ 向量相似度检索（512维）
- ✅ tiktoken 用于 token 计算和内容分段

**功能清单**:
```python
# 新增模块
✅ embeddings/generator.py    # 调用 sentence-transformers 生成向量
✅ embeddings/store.py         # ChromaDB 操作
✅ embeddings/searcher.py      # 向量检索
✅ embeddings/chunker.py       # 内容分段（tiktoken）

# 升级 CRUD
create_memory(title, content, tags)
  → 新增：自动生成 embedding 存入 ChromaDB
  → 新增：长内容自动分段（按 \n\n，超 500 tokens 强制切分）

search_by_query(query, top_k=3)
  → 新增：向量语义搜索
  → 返回：(memory, similarity_score)
```

**架构设计**:
```
┌─────────────────────────────────────┐
│         Memory CRUD (SQLite)         │
│  - title, content, tags, created_at  │
└──────────────┬──────────────────────┘
               │ memory_id
               ▼
┌─────────────────────────────────────┐
│      Vector Store (ChromaDB)         │
│  - embedding (512维向量)             │
│  - metadata: {memory_id, tags}       │
└─────────────────────────────────────┘
```

**验证方式**:
- 存储测试数据，验证检索相关度
- 对比纯文本搜索 vs 语义搜索效果
- 测试模型未下载时的自动下载（首次运行）

**交付物**:
- [embeddings/__init__.py](embeddings/__init__.py)
- [embeddings/generator.py](embeddings/generator.py) - sentence-transformers 封装
- [embeddings/store.py](embeddings/store.py) - ChromaDB 操作
- [embeddings/searcher.py](embeddings/searcher.py) - 向量检索
- [tests/test_embeddings.py](tests/test_embeddings.py)

**成功标准**:
- [ ] 能够成功生成 512 维向量
- [ ] 向量检索返回相关度排序的结果
- [ ] 相似度评分合理（0~1 之间）
- [ ] 首次运行自动下载模型（80MB）

---

### v0.3 - API 服务层

**目标**：提供 HTTP API 接口

**时间**: 第 2-3 周

**技术范围**:
- ✅ FastAPI 0.109+ + Uvicorn 0.27+
- ✅ SQLAlchemy 2.0+ 异步模式升级（AsyncSession）
- ✅ Pydantic 2.5+ 数据验证
- ✅ 自动生成 OpenAPI 文档（Swagger/ReDoc）
- ✅ CORS 配置支持前端调用

**功能清单**:
```python
# API 端点
✅ POST   /api/v1/memories         # 存储记忆
✅ GET    /api/v1/memories/:id     # 获取详情
✅ GET    /api/v1/memories         # 列表查询
✅ DELETE /api/v1/memories/:id     # 删除记忆
✅ POST   /api/v1/search           # 语义搜索
✅ GET    /api/v1/health           # 健康检查
✅ GET    /api/v1/stats            # 统计信息
```

**API 设计**:

**存储记忆**:
```json
POST /api/v1/memories

Request:
{
  "title": "修复生产环境内存泄露",
  "content": "问题现象：服务频繁OOM...",
  "tags": ["bug", "内存", "golang"]
}

Response: 201 Created
{
  "id": 123,
  "title": "修复生产环境内存泄露",
  "tags": ["bug", "内存", "golang"],
  "created_at": "2025-12-25T10:30:00Z"
}
```

**语义搜索**:
```json
POST /api/v1/search

Request:
{
  "query": "我之前怎么解决内存泄露的？",
  "filters": {
    "tags": ["bug"],
    "days": 30
  },
  "limit": 3
}

Response: 200 OK
{
  "results": [
    {
      "id": 123,
      "title": "修复生产环境内存泄露",
      "content": "问题现象：服务频繁OOM...",
      "tags": ["bug", "内存", "golang"],
      "created_at": "2025-12-20T10:30:00Z",
      "relevance": 0.92
    }
  ]
}
```

**验证方式**:
- 自动生成的 Swagger 文档可访问：`http://localhost:8000/docs`
- Postman/curl 测试所有端点
- 性能测试：响应时间 < 200ms（不含向量化）
- 错误处理测试：缺失字段、无效数据

**交付物**:
- [api/__init__.py](api/__init__.py)
- [api/main.py](api/main.py) - FastAPI 应用入口
- [api/routers/__init__.py](api/routers/__init__.py)
- [api/routers/memories.py](api/routers/memories.py) - 记忆相关接口
- [api/routers/health.py](api/routers/health.py) - 健康检查接口
- [api/schemas.py](api/schemas.py) - Pydantic 2.5+ 模型
- [api/dependencies.py](api/dependencies.py) - 依赖注入
- [api/database.py](api/database.py) - 异步数据库会话
- [tests/test_api.py](tests/test_api.py) - API 测试（httpx）

**成功标准**:
- [ ] 所有端点响应正常
- [ ] Swagger 文档完整准确
- [ ] 请求验证生效（无效数据返回 400）
- [ ] CORS 配置正确（支持前端调用）
- [ ] 响应时间 < 200ms（不含向量化）

---

### v0.4 - CLI 启动工具

**目标**：提供友好的命令行启动方式

**时间**: 第 3 周

**技术范围**:
- ✅ Typer 0.9+ + Rich 13+
- ✅ 配置管理（YAML/TOML）
- ✅ 后台服务管理
- ✅ 日志系统（Python logging）

**功能清单**:
```bash
# 命令行接口
✅ ai-memory start      # 启动服务
✅ ai-memory stop       # 停止服务
✅ ai-memory restart    # 重启服务
✅ ai-memory status     # 查看状态
✅ ai-memory init       # 初始化数据库
✅ ai-memory logs       # 查看日志
✅ ai-memory --version  # 版本信息
✅ ai-memory --help     # 帮助信息
```

**配置文件设计**:
```yaml
# ~/.ai-memory/config.yaml
server:
  host: "127.0.0.1"
  port: 8000

database:
  path: "~/.ai-memory/data.db"

vector:
  provider: "sentence-transformers"  # sentence-transformers | jina
  sentence_transformers:
    model: "BAAI/bge-small-zh-v1.5"  # 512维
    cache_dir: "~/.ai-memory/models"  # 模型缓存目录
  jina:
    api_key: "your-api-key"
    model: "jina-embeddings-v2-base-en"

logging:
  level: "INFO"
  file: "~/.ai-memory/logs/app.log"
```

**终端输出示例**:
```
$ ai-memory start

╭──────────────────────────────────────────╮
│   🚀 AI Memory Hub v0.4                  │
╰──────────────────────────────────────────╯

✓ 数据库已就位
✓ 向量服务已连接
✓ API 服务启动中...

╭──────────────────────────────────────────╮
│   🎉 服务已启动                           │
╜──────────────────────────────────────────┤
│   API 地址: http://127.0.0.1:8000        │
│   文档地址: http://127.0.0.1:8000/docs   │
│   PID: 12345                             │
╰──────────────────────────────────────────╯

按 Ctrl+C 停止服务
```

**验证方式**:
- 本地启动服务，访问 API
- 查看 Rich 美化的输出（进度条、表格）
- 测试配置文件加载
- 测试后台进程管理

**交付物**:
- [cli/__init__.py](cli/__init__.py)
- [cli/main.py](cli/main.py) - Typer 应用
- [cli/config.py](cli/config.py) - 配置管理
- [cli/server.py](cli/server.py) - 服务控制
- [cli/utils.py](cli/utils.py) - Rich 输出工具
- [README.md](README.md) - 快速开始指南

**成功标准**:
- [ ] 所有命令正常运行
- [ ] 配置文件正确加载
- [ ] 后台服务可正常启停
- [ ] 日志正确输出
- [ ] 帮助信息完整

---

### v0.5 - Skills/MCP 集成

**目标**：与 AI 编辑器联调，验证端到端流程

**时间**: 第 3-4 周

**技术范围**:
- ✅ Cursor Skills 规范
- ✅ Claude Code MCP 协议
- ✅ AI 交互逻辑优化
- ✅ 错误处理和降级

**功能清单**:
```python
# Skill 实现
✅ skill_save_memory()    # AI 触发存储
✅ skill_search_memory()  # AI 触发搜索
✅ skill_list_memories()  # AI 触发列表
✅ skill_delete_memory()  # AI 触发删除

# AI 提示词优化
✅ 自动提取标题（总结内容）
✅ 自动生成标签（关键词提取）
✅ 智能总结内容（去除冗余）
```

**Cursor Skill 实现**:
```python
# skills/cursor_skill.py

def save_to_memory(conversation: str) -> str:
    """
    AI 调用此函数保存对话经验

    Args:
        conversation: 对话内容

    Returns:
        保存结果消息
    """
    # 1. AI 分析对话，提取关键信息
    # 2. 调用 API 存储到数据库
    # 3. 返回成功消息给 AI
    pass

def search_memory(query: str) -> List[Dict]:
    """
    AI 调用此函数搜索历史经验

    Args:
        query: 自然语言查询

    Returns:
        相关的记忆列表
    """
    pass
```

**Claude MCP 实现**:
```python
# skills/claude_mcp.py

from mcp.server import Server

app = Server("ai-memory-hub")

@app.call_tool()
async def save_memory(title: str, content: str, tags: List[str]):
    """保存记忆到知识库"""
    async with httpx.AsyncClient() as client:
        response = await client.post(
            "http://localhost:8000/api/v1/memories",
            json={"title": title, "content": content, "tags": tags}
        )
    return response.json()

@app.call_tool()
async def search_memory(query: str, limit: int = 3):
    """搜索知识库"""
    async with httpx.AsyncClient() as client:
        response = await client.post(
            "http://localhost:8000/api/v1/search",
            json={"query": query, "limit": limit}
        )
    return response.json()
```

**测试场景**:

**场景 1：Bug 修复记录**
```
用户在 Cursor 中解决了一个内存泄露问题
└─> 用户说："把这个解决方案记录一下"
└─> AI 调用 save_to_memory()
└─> 返回："✓ 已保存：修复生产环境内存泄露"
└─> 用户说："我之前怎么处理内存泄露的？"
└─> AI 调用 search_memory()
└─> 返回历史方案
```

**场景 2：跨会话验证**
```
会话 1: 记录一个部署问题的解决方案
└─> 重启 Cursor
会话 2: 搜索 "部署 502 怎么处理的？"
└─> 仍能找到会话 1 的记录
```

**验证方式**:
- 在 Cursor 中实测：记录 bug 修复方案
- 在 Claude Code 中实测：搜索历史经验
- 跨会话验证：重启编辑器后数据仍可检索
- 错误处理：服务未启动时的提示

**交付物**:
- [skills/__init__.py](skills/__init__.py)
- [skills/cursor_skill.py](skills/cursor_skill.py) - Cursor Skill
- [skills/claude_mcp.py](skills/claude_mcp.py) - Claude MCP Server
- [skills/prompts.py](skills/prompts.py) - AI 提示词模板
- [docs/CURSOR_INTEGRATION.md](docs/CURSOR_INTEGRATION.md) - 集成文档
- [docs/CLAUDE_MCP_INTEGRATION.md](docs/CLAUDE_MCP_INTEGRATION.md)

**成功标准**:
- [ ] 在 Cursor 中成功存储和搜索
- [ ] 在 Claude Code 中成功存储和搜索
- [ ] 跨会话数据可用
- [ ] AI 能够理解返回的结果并正确引用
- [ ] 错误时有友好的提示信息

---

### v0.6 - Web UI

**目标**：提供可视化管理界面

**时间**: 第 4-5 周

**技术范围**:
- ✅ React 18 + TypeScript 5+
- ✅ Vite 5+ 构建工具
- ✅ Ant Design 5+ 组件库
- ✅ Tailwind CSS 3+ 原子化样式
- ✅ FastAPI 静态文件服务
- ✅ 响应式布局

**功能清单**:
```tsx
// 页面组件
✅ HomePage       # 首页（列表 + 搜索）
✅ SearchBar      # 搜索框（自然语言）
✅ FilterBar      # 过滤器（标签/时间）
✅ MemoryCard     # 记录卡片
✅ DetailModal    # 详情弹窗
✅ DeleteConfirm  # 删除确认
✅ EmptyState     # 空状态提示
✅ LoadingSpinner # 加载动画
```

**页面布局设计**:
```
┌─────────────────────────────────────────────────────┐
│  AI Memory Hub                           [⚙️] [❓] │
├─────────────────────────────────────────────────────┤
│  🔍 [我之前怎么解决内存泄露的？          ] [搜索]  │
│  🏷️ 标签: [全部 ▼]  📅 时间: [全部 ▼]               │
├─────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────┐  │
│  │ 📌 修复生产环境内存泄露       [2025-12-20]     │  │
│  │ 🏷️ bug · 内存 · golang                       │  │
│  │                                               │  │
│  │ 问题现象：服务频繁OOM...                       │  │
│  │                    [详情] [删除]               │  │
│  └───────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────┐  │
│  │ 📌 Golang 内存优化技巧         [2025-12-15]   │  │
│  │ 🏷️ 性能 · 内存                                │  │
│  │                    [详情] [删除]               │  │
│  └───────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────┤
│  共 23 条记录                   1 / 3 页 [›] [»]     │
└─────────────────────────────────────────────────────┘
```

**组件结构**:
```
src/
├── App.tsx                 # 主应用
├── pages/
│   └── HomePage.tsx        # 首页
├── components/
│   ├── SearchBar.tsx       # 搜索框
│   ├── FilterBar.tsx       # 过滤器
│   ├── MemoryCard.tsx      # 记录卡片
│   ├── DetailModal.tsx     # 详情弹窗
│   ├── DeleteConfirm.tsx   # 删除确认
│   └── EmptyState.tsx      # 空状态
├── hooks/
│   ├── useMemories.ts      # 记忆数据
│   └── useSearch.ts        # 搜索逻辑
├── services/
│   └── api.ts              # API 调用
└── types/
    └── index.ts            # TypeScript 类型
```

**API 集成**:
```typescript
// services/api.ts

const BASE_URL = '/api/v1';

export async function searchMemories(
  query: string,
  filters?: SearchFilters
): Promise<Memory[]> {
  const response = await fetch(`${BASE_URL}/search`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ query, filters, limit: 10 })
  });
  return response.json();
}

export async function deleteMemory(id: number): Promise<void> {
  await fetch(`${BASE_URL}/memories/${id}`, {
    method: 'DELETE'
  });
}
```

**验证方式**:
- 浏览器访问 `http://localhost:8000`
- 完成一次完整的存储→搜索→删除流程
- 移动端兼容性测试（响应式布局）
- 浏览器兼容性测试（Chrome, Firefox, Safari）

**交付物**:
- [frontend/src/](frontend/src/) - React 源码
- [frontend/dist/](frontend/dist/) - 构建产物
- [frontend/index.html](frontend/index.html) - 入口 HTML
- [frontend/vite.config.ts](frontend/vite.config.ts) - Vite 5+ 配置
- [frontend/tailwind.config.js](frontend/tailwind.config.js) - Tailwind CSS 配置
- [frontend/tsconfig.json](frontend/tsconfig.json) - TypeScript 5+ 配置
- [frontend/package.json](frontend/package.json) - 依赖配置
- [api/static.py](api/static.py) - 静态文件服务集成

**成功标准**:
- [ ] 页面正常渲染，无控制台错误
- [ ] 搜索功能正常工作
- [ ] 过滤器正确筛选数据
- [ ] 删除操作有确认和反馈
- [ ] 移动端布局正常
- [ ] 首屏加载时间 < 1s

---

### v0.7 - 异步任务优化

**目标**：向量化不阻塞 API 响应

**时间**: 第 5 周

**技术范围**:
- ✅ asyncio.Queue 任务队列
- ✅ 后台 worker 线程
- ✅ 任务状态追踪
- ✅ 失败重试机制

**功能清单**:
```python
# 异步处理
✅ POST /memories -> 立即返回（201 Accepted）
✅ 后台异步生成 embedding
✅ 轮询查询任务状态
✅ 失败自动重试（最多 3 次）
✅ 任务日志记录
```

**架构设计**:
```
┌─────────────────────────────────────────────────┐
│              API Request                        │
│         POST /api/v1/memories                   │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│         立即返回 201 Accepted                    │
│         { "task_id": "abc-123",                 │
│           "status": "processing" }              │
└─────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│         asyncio.Queue (内存队列)                 │
│         Task { memory_id, type, retry }         │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│         Background Worker (后台线程)             │
│         - 从队列取任务                           │
│         - 调用 Ollama 生成向量                   │
│         - 存储到 ChromaDB                        │
│         - 更新任务状态                           │
└─────────────────────────────────────────────────┘
```

**API 变更**:
```json
POST /api/v1/memories

# 之前（同步）：
# 等待向量化完成，响应时间 2-5s

# 现在（异步）：
Response: 202 Accepted
{
  "id": 123,
  "task_id": "abc-123",
  "status": "processing",
  "message": "记忆已保存，正在生成向量索引..."
}

GET /api/v1/tasks/abc-123

Response: 200 OK
{
  "task_id": "abc-123",
  "status": "completed",  # processing | completed | failed
  "created_at": "2025-12-25T10:30:00Z",
  "completed_at": "2025-12-25T10:30:03Z",
  "error": null
}
```

**Worker 实现**:
```python
# tasks/worker.py

import asyncio
from queue import Queue

class EmbeddingWorker:
    def __init__(self, queue: Queue):
        self.queue = queue
        self.running = False

    async def start(self):
        """启动后台 worker"""
        self.running = True
        while self.running:
            task = await self.queue.get()
            try:
                await self.process_task(task)
            except Exception as e:
                await self.handle_failure(task, e)

    async def process_task(self, task):
        """处理单个任务"""
        # 1. 生成 embedding
        embedding = await generate_embedding(task.content)

        # 2. 存储到 ChromaDB
        await store_embedding(task.memory_id, embedding)

        # 3. 更新任务状态
        await update_task_status(task.id, "completed")
```

**验证方式**:
- API 响应时间 < 100ms
- 并发存储 10 条记录不阻塞
- 任务失败后自动重试
- Worker 崩溃后任务不丢失（日志记录）

**交付物**:
- [tasks/__init__.py](tasks/__init__.py)
- [tasks/queue.py](tasks/queue.py) - 任务队列
- [tasks/worker.py](tasks/worker.py) - 后台 worker
- [tasks/models.py](tasks/models.py) - 任务模型
- [tests/test_tasks.py](tests/test_tasks.py)

**成功标准**:
- [ ] API 响应时间 < 100ms
- [ ] 任务成功率 > 95%
- [ ] 失败任务自动重试
- [ ] 任务状态可查询
- [ ] 日志完整记录

---

### v0.8 - 打包与分发

**目标**：用户可以一键安装运行

**时间**: 第 5-6 周

**技术范围**:
- ✅ PyInstaller 6+ 打包
- ✅ 多平台编译（macOS, Windows, Linux）
- ✅ 安装文档和示例
- ✅ 版本管理

**功能清单**:
```bash
# 分发方式
✅ GitHub Releases    # 二进制文件
✅ pip install        # PyPI 发布
✅ Docker Image       # 可选，面向高级用户
```

**PyInstaller 配置**:
```python
# build.spec

# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['cli/main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('api/templates', 'api/templates'),
        ('frontend/dist', 'frontend/dist'),
    ],
    hiddenimports=[
        'uvicorn',
        'uvicorn.logging',
        'sqlalchemy',
        'sqlalchemy.dialects.sqlite',
        'chromadb',
        'sentence_transformers',
        'transformers',
        'tiktoken',
        'typer',
        'rich',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='ai-memory',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
```

**打包流程**:
```bash
# 1. 构建 React 前端
cd frontend && npm run build

# 2. PyInstaller 打包
pyinstaller build.spec --onefile

# 3. 测试可执行文件
./dist/ai-memory --version
./dist/ai-memory start

# 4. 创建 GitHub Release
gh release create v0.8 \
  ./dist/ai-memory-macos \
  ./dist/ai-memory-windows.exe \
  ./dist/ai-memory-linux
```

**安装文档**:
```markdown
# 安装指南

## 方式 1：下载二进制文件（推荐）

1. 访问 [Releases](https://github.com/xxx/ai-memory-hub/releases)
2. 下载对应系统的文件：
   - macOS: `ai-memory-macos`
   - Windows: `ai-memory-windows.exe`
   - Linux: `ai-memory-linux`
3. 添加执行权限（macOS/Linux）：
   ```bash
   chmod +x ai-memory-macos
   ```
4. 运行：
   ```bash
   ./ai-memory-macos start
   ```

## 方式 2：pip 安装

```bash
pip install ai-memory-hub
ai-memory start
```

## 首次运行

首次运行会自动：
1. 创建数据目录 `~/.ai-memory/`
2. 初始化 SQLite 数据库
3. 生成配置文件 `~/.ai-memory/config.yaml`
4. 自动下载 embedding 模型（80MB，首次运行约需 1-2 分钟）

## 验证安装

```bash
$ ai-memory --version
AI Memory Hub v0.8

$ ai-memory status
✓ 服务运行中 (PID: 12345)
✓ API 地址: http://127.0.0.1:8000
```
```

**验证方式**:
- 在干净的 macOS 环境测试
- 在干净的 Windows 环境测试
- 在干净的 Linux 环境测试
- 验证所有功能正常

**交付物**:
- [build.spec](build.spec) - PyInstaller 配置
- [Dockerfile](Dockerfile) - Docker 配置
- [setup.py](setup.py) - PyPI 配置
- [dist/](dist/) - 编译产物
- [docs/INSTALL.md](docs/INSTALL.md) - 安装文档
- GitHub Release

**成功标准**:
- [ ] 三个平台都能成功打包
- [ ] 双击可执行文件即可启动
- [ ] 首次运行自动初始化
- [ ] 所有功能正常工作
- [ ] 安装文档清晰易懂

---

### v1.0 - 正式发布

**目标**：生产环境可用

**时间**: 第 6 周

**内容**:
- ✅ 集成 v0.1 ~ v0.8 所有功能
- ✅ 完善文档和示例
- ✅ 性能优化和 Bug 修复
- ✅ 用户反馈收集机制
- ✅ 发布说明和宣传

**功能清单**:
```
✅ 数据存储（SQLite + ChromaDB）
✅ 语义搜索（Embedding + 向量检索）
✅ REST API（FastAPI）
✅ CLI 工具（Typer + Rich）
✅ Skills/MCP 集成
✅ Web UI（React）
✅ 异步任务（asyncio.Queue）
✅ 可执行文件打包
```

**性能基线**:
| 指标 | 目标 | 实际 |
|------|------|------|
| 存储响应时间 | < 100ms | ___ |
| 搜索响应时间 | < 500ms | ___ |
| Web UI 首屏 | < 1s | ___ |
| 内存占用（空闲） | < 200MB | ___ |
| 启动时间 | < 3s | ___ |

**交付物**:
- [CHANGELOG.md](CHANGELOG.md) - 更新日志
- [README.md](README.md) - 项目说明
- [docs/](docs/) - 完整文档
- [examples/](examples/) - 使用示例
- GitHub Release v1.0
- PyPI 发布
- 官网/宣传文章

**发布检查清单**:
- [ ] 所有测试通过
- [ ] 性能达标
- [ ] 文档完整
- [ ] 三个平台打包成功
- [ ] 安全审查（无敏感信息）
- [ ] 许可证文件
- [ ] 贡献指南
- [ ] 问题模板

---

## 5. 迭代逻辑总结

### 5.1 版本依赖关系

```
v0.1 (Core 层)
    ↓
v0.2 (向量化)
    ↓
v0.3 (API 层)
    ↓
    ├─→ v0.4 (CLI)
    └─→ v0.5 (Skills)
        ↓
    v0.6 (Web UI)
        ↓
    v0.7 (异步)
        ↓
    v0.8 (打包)
        ↓
    v1.0 (发布)
```

### 5.2 技术风险分析

| 版本 | 核心目标 | 技术风险 | 依赖关系 |
| v0.1 | 跑通数据层 | 🟢 低 | 无 |
| v0.2 | 加入向量能力 | 🟡 中 | v0.1 |
| v0.3 | 暴露 API | 🟢 低 | v0.1 |
| v0.4 | CLI 工具 | 🟢 低 | v0.3 |
| v0.5 | AI 编辑器联调 | 🟠 高 | v0.3 + v0.2 |
| v0.6 | Web UI | 🟡 中 | v0.3 |
| v0.7 | 异步优化 | 🟡 中 | v0.3 |
| v0.8 | 打包分发 | 🟠 中 | v0.1 ~ v0.7 |

### 5.3 可交付里程碑

| 里程碑 | 版本 | 可演示功能 |
|--------|------|----------|
| M1 | v0.1 | 命令行存储和查询记忆 |
| M2 | v0.2 | 语义搜索展示 |
| M3 | v0.3 | API 文档和接口测试 |
| M4 | v0.4 | CLI 启动服务 |
| M5 | v0.5 | Cursor 中实际使用 |
| M6 | v0.6 | Web UI 可视化管理 |
| M7 | v0.7 | 高并发性能测试 |
| M8 | v0.8 | 独立安装包 |
| M9 | v1.0 | 正式发布 |

---

## 6. 时间规划

### 6.1 6 周开发计划

```
Week 1                    Week 2                    Week 3
├─ v0.1 Core 层           ├─ v0.2 向量化            ├─ v0.4 CLI
│  └─ 数据库 + CRUD       │  └─ ChromaDB + Ollama   │  └─ 启动工具
├─ v0.3 API 层            ├─ v0.3 API 层            ├─ v0.5 Skills
│  └─ FastAPI 接口        │  └─ 测试 + 完善         │  └─ AI 联调

Week 4                    Week 5                    Week 6
├─ v0.5 Skills            ├─ v0.6 Web UI            ├─ v0.8 打包
│  └─ 测试 + 完善         ├─ v0.7 异步              │  └─ 多平台编译
                          │  └─ 任务队列            ├─ v1.0 发布
                          │                         │  └─ 文档 + 宣传
```

### 6.2 人力分配

**假设：2 名开发者（A 和 B）**

| 版本 | 开发者 A | 开发者 B | 协作点 |
|------|---------|---------|--------|
| v0.1 | Core 层开发 | 测试框架 | 接口对齐 |
| v0.2 | Embedding 集成 | ChromaDB 调研 | 数据同步 |
| v0.3 | API 开发 | API 测试 | 接口文档 |
| v0.4 | CLI 工具 | 配置管理 | 启动脚本 |
| v0.5 | Cursor Skill | Claude MCP | 联调测试 |
| v0.6 | React 前端 | UI 设计 | API 对接 |
| v0.7 | 异步队列 | Worker 实现 | 状态管理 |
| v0.8 | 打包脚本 | 多平台测试 | 安装文档 |

---

## 7. 风险管理

### 7.1 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| **ChromaDB 性能瓶颈** | 高 | 中 | 性能测试验证，保留切换到 Qdrant 的选项 |
| **模型首次下载慢** | 低 | 高 | 文档提示（80MB，约 1-2 分钟），进度条显示 |
| **PyInstaller 打包失败** | 高 | 中 | 提前验证各平台打包流程 |
| **异步任务丢失** | 中 | 中 | 日志记录，v2.0 升级 Celery |
| **Skills 联调失败** | 高 | 低 | 预留调试时间，参考官方示例 |

### 7.2 时间风险

| 风险 | 应对 |
|------|------|
| 某个版本延期 | 评估是否可合并到下一版本 |
| 技术难点卡住 | 寻求社区帮助，或降级方案 |
| 测试发现问题 | 预留 20% Buffer 时间 |

### 7.3 降级方案

| 组件 | 正常方案 | 降级方案 |
|------|---------|---------|
| 向量数据库 | ChromaDB | 纯文本搜索 |
| Embedding | sentence-transformers 本地 | Jina AI 远程 |
| 前端 | React SPA | 纯 HTML 页面 |
| 异步队列 | asyncio.Queue | 同步处理（慢但可用） |
| 打包 | PyInstaller | Docker 容器 |

---

## 8. 成功指标

### 8.1 技术指标

| 指标 | 目标 | 测量方法 |
|------|------|----------|
| 测试覆盖率 | > 80% | pytest |
| API 响应时间 | < 200ms | Apache Bench |
| 搜索准确率 | Top 1 > 0.8 | 人工评估 |
| 内存占用 | < 200MB | psutil |
| 启动时间 | < 3s | time 命令 |

### 8.2 产品指标

| 指标 | 目标 |
|------|------|
| 核心功能完成度 | 100% |
| 文档完整度 | 100% |
| 跨平台支持 | macOS, Windows, Linux |
| 用户上手时间 | < 5 分钟 |

---

## 9. 后续版本规划

### v1.1 - 稳定性增强

- [ ] 数据导出（Markdown/JSON）
- [ ] 数据备份和恢复
- [ ] 任务持久化（升级 Celery）
- [ ] 日志轮转

### v1.2 - 功能增强

- [ ] 内容编辑
- [ ] 标签管理（合并、重命名）
- [ ] 批量操作
- [ ] 高级过滤（组合条件）

### v2.0 - 多用户与知识图谱

- [ ] 用户认证和授权
- [ ] 多用户数据隔离
- [ ] 知识图谱可视化
- [ ] 关联推荐
- [ ] 统计面板
- [ ] PostgreSQL 升级

---

## 10. 附录

### 10.1 参考资料

- [技术选型文档](./技术选型文档.md)
- [产品需求文档](./AI知识记忆库 产品需求文档 (PRD).md)

### 10.2 版本记录

| 版本 | 日期 | 变更说明 | 作者 |
|------|------|----------|------|
| v1.0 | 2025-12-25 | 初始版本 | 产品组 |

---

**文档结束**
